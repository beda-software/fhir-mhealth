import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    safeArea: {
        flexGrow: 1,
    },
    container: {
        flexGrow: 1,
        padding: 30,
    },
    label: {
        fontSize: 24,
        lineHeight: 30,
    },
});

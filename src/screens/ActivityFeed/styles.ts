import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    safeArea: {
        flexGrow: 1,
    },
    container: {
        padding: 30,
    },
    scrollView: {
        flexBasis: 1,
    },
    label: {
        fontSize: 24,
        lineHeight: 30,
    },
    controlsContainer: {
        paddingTop: 10,
        gap: 15,
        alignItems: 'center',
    },
});
